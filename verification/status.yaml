schema_version: 1
baseline_commit: "f80269b"
generated_at: "2026-07-22T20:55:45+03:00"
statuses:
  LOCAL_GREEN:
    state: GREEN
    evidence: "go test ./..., go test -race ./..., go vet ./..., architecture and system-E2E harness tests, 157/157 current pivot matrix with 1104 Go tests, OpenTofu validate for all four stacks, and zero-drift live plans for admin/network/Cozystack smoke"
  DB_GREEN:
    state: GREEN
    evidence: "Iteration 5 regression plus isolated managed PostgreSQL integration and K14 gates in ai_native_paas_integration_test; the gate runner refuses the production database"
  PROVIDER_GREEN:
    state: PENDING
    evidence: "The two preserved IPv4s, runtime/workspace VPCs, shared NAT, runtime LB and private Talos/Cozystack smoke cell are live; the compact workspace QCOW2, SPDX SBOM and keyless Sigstore bundles passed exact-revision verification and the QCOW2 is imported as Timeweb custom image 803212b3-aa74-4c13-9bdf-77b864216994; private-VPC boot and disposable destroy evidence remain pending; capability gateway admission fails closed on missing provider leases and maps provider usage into Commerce meters; GitLab and capability-provider live suites remain incomplete"
  DEV_PRODUCT_GREEN:
    state: PENDING
    evidence: "runtime_sim_k8s is accepted as the default cheap development runtime path; runtime-api process smoke proves create application -> GitOps commit -> cells/runtime-sim-k8s path -> sim.runtime.internal hostname -> status/cross-tenant checks, acceptance tests cover agent task -> project create -> repository patch -> build -> approval -> runtime_sim_k8s hostname -> probe evidence -> usage preview -> workspace destroy evidence -> redacted audit, Project MCP build_execute queues only the governed workspace-agent verified-build command for the workspace-bound exact SHA and BuildSpec fingerprint, workspace-agent plans/runs verified rootless BuildKit, and agent-api now has mTLS-bound Runtime and Attachments bridges. Internal Harbor uses a dedicated single-bucket S3 user and has a live private OCI/S3 round trip plus post-revoke robot read-denial gate; the application adapter remains unconfigured until production robot-lease issuance, OpenBao signing and a live workspace receipt. Governed legacy source compatibility, live workspace VM, GitLab MCP and Argo/Cozystack remain pending"
  K8S_GREEN:
    state: PENDING
    evidence: "Talos/Cozystack/Envoy/LINSTOR/Argo and 20 consecutive GitOps hello-go HTTP probes are green in the smoke profile; runtime_sim_k8s manifests are ready for cheap dev product runs; live provider_gate PostgreSQL/Redis/S3 lifecycle and backup/restore gates remain pending"
  COZYSTACK_LIVE_GREEN:
    state: PENDING
    evidence: "Real Cozystack evidence remains a live release gate only: Talos install, Cozystack installer, tenant namespaces, LINSTOR on dedicated nodes, apps.cozystack.io Redis/Bucket and runtime edge evidence cannot be replaced by runtime_sim_k8s"
  PROVIDER_FULL_GREEN:
    state: PENDING
    evidence: "provider_gate_full is defined and tested as a paid live window, but Redis/Bucket/S3 high-level Cozystack lifecycle and backup/restore are blocked until Timeweb account balance allows three provider_gate_full VMs"
  SECURITY_GREEN:
    state: PENDING
    evidence: "No runtime node has floating IPv4, Talos 50000 is not publicly exposed, provider firewalls default DROP, tracked secret sentinel passes, and internal APIs have a tested TLS 1.3/SPIFFE service boundary that rejects unverified clients and forgeable principal headers. The account-wide Timeweb S3 secret was rotated and its old pair is rejected. Development diagnostics exposed the active Timeweb provider token plus workspace-log and image-staging scoped S3 pairs; verification/PRE_BETA_CREDENTIAL_ROTATION.md allows development-only gates to continue but blocks every beta window until rotation, consumer resync, old-key rejection and the full sentinel are green. Live OpenBao service-certificate mounts and workspace compromise/isolation gates remain pending"
  OPS_GREEN:
    state: PENDING
    evidence: "State-service scoped HTTP credentials were rotated after expiry and its dedicated S3 user passed a real encrypted-state read; workspace logs, image staging and Harbor also use dedicated single-bucket users. Network-foundation=live and cozystack-lab=smoke zero-drift plans pass, earlier NATS/OpenBao backup evidence is retained, Cozystack smoke evidence is recorded, the workspace-manager OpenBao Kubernetes role has a live custom-audience direct-auth and Agent Injector gate with Transit denial, Project MCP production tools persist task evidence through agent PostgreSQL, Project MCP build_execute is bound to workspace source identity and BuildSpec fingerprint, workspace-agent verified-build has an executable rootless BuildKit boundary, agent-api can use Commerce, Kernel, Build, Runtime and Attachments APIs through a fail-closed TLS 1.3/SPIFFE mTLS boundary, and capability gateway admission/usage core is covered by local tests; service-certificate deployment, Cozystack backups, live restore and full chaos gates remain incomplete"

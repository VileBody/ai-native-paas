# Admin full-standby gate — 2026-07-16

Target: Timeweb project `BALOVSTVO`, managed cluster `ai-native-paas-test`
(`1099941`).

## Preconditions

- OpenBao HA/failover and isolated restore completed; the final encrypted Raft
  snapshot has a verified S3 version and SHA-256.
- NATS R3 leader failover/deduplication completed; all five streams were
  exported, encrypted and verified from a versioned S3 download.
- `state-service` was moved to the retained CI worker before the infrastructure
  plan was applied.

OpenBao and NATS Helm releases were then reconciled to `off`. The OpenBao server
StatefulSet and NATS StatefulSet are `0/0`. The OpenBao injector component and
its admission webhook are absent, and the NATS helper is absent. Six OpenBao
PVCs and three NATS PVCs remain `Bound`.

The live gate found and fixed an unsafe first version of the OpenBao off profile:
zero injector replicas had left a `failurePolicy=Fail` admission webhook, which
blocked new Pods. Off mode now disables the injector chart component and fails
its deploy script if the webhook remains.

## Exact infrastructure change

The final reviewed OpenTofu plan reported:

```text
Plan: 0 to add, 0 to change, 1 to destroy.
```

Its only action was deletion of `twc_k8s_node_group.system[0]`, Timeweb node
group `117653`, containing three 4-vCPU/8-GiB workers. Applying that exact saved
plan completed with `0 added, 0 changed, 1 destroyed`. A new live refresh after
apply reported `No changes`.

Preserved resources include cluster `1099941`, CI node group `117651`, managed
PostgreSQL `4187905`, the VPC/router and DNAT rules, S3 buckets, public edge IP,
PVCs, state backend and all quarantined legacy apps.

## Single-node health and cost

- exactly one Kubernetes node remains: Ready CI worker
  `worker-192.168.73.5`;
- the CSI controller and node plugin, CoreDNS and `state-service` are Ready on
  that worker;
- no Pending or Failed Pods remained after reconciliation;
- the managed CSI controller uses reduced standby requests and CoreDNS uses a
  single-node-safe replacement strategy;
- Argo CD reports both `rec-sidecar` and `vietnam-rent` `Synced/Healthy`;
- the post-gate node snapshot was 140 millicores (7%) and 1,709 MiB (44%);
- `rec.teamgenius.ru`, `vietnam.teamgenius.ru`,
  `suffleur.teamgenius.ru` and `klukhanova.teamgenius.ru` all returned HTTP
  `200` after the deletion.

The Timeweb account finance API reported:

```text
monthly_cost: 15591 RUB
hourly_cost: 21.3 RUB
balance: 11357.39 RUB
hours_left: 533
```

The monthly total immediately before deleting the system group was 25,311 RUB.
The realized nominal reduction is therefore **9,720 RUB/month**, matching three
workers at 3,240 RUB/month each.

## Resume order

1. Apply admin OpenTofu with `admin_capacity_mode=ha` and wait for three system
   workers to become Ready.
2. Run `ADMIN_CAPACITY_MODE=ha scripts/reconcile-timeweb-csi.sh`.
3. Deploy OpenBao in `ha`, unseal each member with any valid three shares and
   require three voting peers.
4. Deploy NATS in `ha`, require all replicas current and verify the stream set.
5. Run the admin zero-drift, application, snapshot and public endpoint gates.

Do not attempt the beta window while the admin profile is `off` or while custody
is still the solo-development profile.
